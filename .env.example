# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# Dashboard: Firebase Realtime Database base URL (read-only public test DB).
VITE_FIREBASE_URL=https://iotelfinal-default-rtdb.firebaseio.com

# Hardhat: on-chain log anchoring (Phase 5). Leave blank to skip Sepolia.
SEPOLIA_RPC_URL=
SEPOLIA_PRIVATE_KEY=
LOCAL_RPC_URL=http://127.0.0.1:8545
